DATABASE_URL="postgresql://hunting_game:hunting_game@localhost:5432/hunting_game?schema=public"
JWT_SECRET="change-this-to-a-long-random-secret-in-production"
JWT_EXPIRES_IN="3650d"
PORT=4000
OVERPASS_API_URL="https://overpass-api.de/api/interpreter"
CORS_ORIGIN="*"
# Where the friend-add landing page (GET /u/:username/:tag) sends someone who doesn't have
# the app yet. A TestFlight public link for now; swap to the real App Store URL once the
# app is actually live there.
TESTFLIGHT_URL="https://testflight.apple.com/join/XH5fVVMV"
